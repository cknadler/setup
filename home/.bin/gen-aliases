#!/usr/bin/env ruby

ws = "/Users/#{ENV["USER"]}/workspace/mshop/src"
exit unless File.exists?(ws)

Dir.foreach(ws) do |f|
  next if f.match(/\..*/)
  als = f.gsub(/[a-z]/, "").downcase
  puts "alias #{als}='cd #{ws}/#{f}';"
end
