# DCSS tiles install moved to bootstrap's `crawl` step: the cask is
# deprecated (disabled 2026-09-01) because the binary isn't Apple-notarized.
# Run: `./bootstrap --only crawl`
cask 'yacreader'
cask 'plex'
